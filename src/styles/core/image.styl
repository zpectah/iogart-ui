img
    border-style none

.{$core.image}
    border 0

    &.{$context.fluid}
        image-responsive()
        border 0

    &{$pfxType}{$core._image_thumbnail}
        image-responsive()
        padding 2px
        background-color white
        border 1px $border-default-style
        border-radius $border-radius-base



